import { Blockchain } from './blockchain';
import { Transaction } from './transaction';

// Create a new blockchain
// const blockchain = new Blockchain(3);

// // Add a new transaction
// const transaction = new Transaction('John', 'Jane', 100);
// blockchain.addTransaction(transaction);

// // Mine the pending transactions
// blockchain.minePendingTransactions('my-wallet-address');

// // Add another transaction
// const anotherTransaction = new Transaction('Jane', 'Bob', 50);
// blockchain.addTransaction(anotherTransaction);

// // Mine the pending transactions again
// blockchain.minePendingTransactions('my-wallet-address');

// Log the current state of the blockchain
